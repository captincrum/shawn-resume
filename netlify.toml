[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback: refreshing /games/find-the-cat serves the app, not a 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200